paths:
  /notifications:
    get:
      tags:
        - Notifications
      summary: User should get all their notifications
      description:
        Authentication required, returns an array of notifications, no additional
        parameter
      produces:
        - application/json
      security:
        - bearerAuth: []
      responses:
        200:
          description: Successful
